/// <reference types="vite/client" />

// Injetadas pelo vite.config.ts (define) a partir de package.json e do
// horário do build — ver Admin > Ambientes.
declare const __APP_VERSION__: string
declare const __BUILD_TIME__: string
